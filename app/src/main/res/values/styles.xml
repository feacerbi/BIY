<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:background">@android:color/white</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme.AppBarOverlay">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="ExoMediaButton.Replay" parent="ExoMediaButton">
        <item name="android:src">@drawable/exo_controls_repeat_all</item>
        <item name="android:contentDescription">@string/exo_controls_repeat_all_description</item>
    </style>

    <style name="ExoMediaButton.Rewind5" parent="ExoMediaButton.Rewind">
        <item name="android:src">@drawable/ic_replay_5_white_32dp</item>
        <item name="android:contentDescription">@string/exo_controls_rewind_description</item>
    </style>

</resources>
